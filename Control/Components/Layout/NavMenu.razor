@implements IDisposable

@inject NavigationManager NavigationManager

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Control</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        @* <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div> *@

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="incidencias">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Incidencias
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="laboratorios">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Laboratorio 
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="asignaciones">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Asignación 
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="equipos">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Equipos
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="historial">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Historial
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="recursos">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Recursos
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="usuarios">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Usuarios
            </NavLink>
        </div>        
    </nav>
</div>

@code {
    private string? currentUrl;

    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}

