@page "/equipos"
@inject IEquiposRepositorio _equiposRepositorio


@if (Cargando)
{
    <div class="poition-absolute w-75 h-75 d-flex flex-column align-items-center bg-white justify-content-center">
        <img src="/Images/Carga.gif" alt="Cargando contenido..." />
    </div>
}
else
{
    <div class="card shadow border-0 mt-4">
        <div class="card-header bg-black bg-gradient m-lg-0 py-3">
            <div class="row">
                <div class="col-12 text-center">
                    <h2 class="text-white py-2">Lista de Productos</h2>
                </div>
            </div>
        </div>

        <div class="card-body py-4">
            <div class="row pb-3">
                <div class="col text-end">
                    <a href="producto/agregar" class="btn btn-secondary" style="width:250px;">
                        <i class="bi bi-plus-circle"></i> Agregar nuevo producto
                    </a>
                </div>
            </div>

            @if (equipos.Any())
            {
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>Nombres</th>
                            <th>Acciones</th>
                            <th>Acciones</th>
                            <th>Acciones</th>
                            <th>Acciones</th>
                            <th>Acciones</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in incidencias)
                        {
                            <tr>
                                <td>@item.IdIncidencias</td>
                                <td>
                                    <a href="@($"producto/modificar/{item.IdIncidencias}")" class="btn btn-primary">
                                        <i class="bi bi-pencil-square"></i> Modificar
                                    </a>

                                    <a href="@($"producto/eliminar/{item.IdIncidencias}")" class="btn btn-danger">
                                        <i class="bi bi-trash"></i> Eliminar
                                    </a>
                                </td>
                                <td>@item.</td>
                                <td>@item.</td>
                                <td>@item.</td>
                                <td>@item.</td>
                                <td>@item.DescInc</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="text-center">No hay productos disponibles</p>
            }
        </div>
    </div>
}

@code {


    private bool Cargando { get; set; } = false;

    private IEnumerable<Incidencias> incidencias { get; set; } = new List<Incidencias>();

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            Cargando = true;
            await CargandoIncidencias();
            Cargando = false;
            StateHasChanged();
        }
    }

    public async Task CargandoIncidencias()
    {
        incidencias = await _incidenciasRepositorio.GetAll();
    }
}
