function generarReporte() {
    // Aquí puedes crear un archivo, como un CSV o PDF, o simplemente abrir una URL con el reporte.
    const reporteUrl = "/reporte-incidencias"; // Asumiendo que esta es la URL donde se genera el reporte
    window.open(reporteUrl, '_blank');
}